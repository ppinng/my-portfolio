{
  "version": 2,
  "builds": [
    {
      "src": "api/*.go",
      "use": "@vercel/go"
    },
    {
      "src": "src/**/*",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/[^.]+", "dest": "/", "status": 200 },
    {
      "src": "/api/(.*)",
      "dest": "/api/main.go"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
